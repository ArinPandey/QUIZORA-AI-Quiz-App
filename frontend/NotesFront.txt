Since the backend code is sort of done, so its FRONTEND TIME!!!!!!!!!!!!!!!!!!

1) Setup the react-tailwind project environment...
2) Setup "concurrently" in the main folder's package...

Now what is concurrently used for ? concurrently is a Node.js tool that lets you run multiple commands concurrently with prefixes, colors, and kill options. Simply, i used it to run the BACKEND as well as FRONTEND simultaneously, without using two different commands respectively...

"react-router-dom" for using Router...
Setup "Routes" from "react-router-dom" in App.jsx and start building the components...

****** Now first code the "Navbar", it is a Global component (or not a route-specific or Local component), means it is present everywhere...



